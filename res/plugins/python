set language_pack = 1;
set extensions = "py python";
set control_flow = "await else pass break except in raise class finally is return and continue for try as def nonlocal while assert del global not with async elif if or yield";
set constants = "True False None";
set types = "str int list dict tuple float lambda";
set misc = "from import";
